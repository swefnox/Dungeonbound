///@func process_field(field, hero)
var player = argument1;
with(argument0)
{
	for(var i = 0; i < array_length_1d(attached); ++i)
	{
		//show_debug_message(string(attached[i]));
		with(attached[i])
		{
			event_user(0);
		}
	}
}